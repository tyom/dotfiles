*.log
.env

# repo-intel is now a standalone tool (github.com/tyom/repo-intel).
# The artifact is installed via Homebrew or fetched into the stow tree
# during setup, not committed here.
stow/bin/repo-intel
